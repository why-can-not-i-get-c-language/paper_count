# 项目进度

最后更新：2026-07-26

## 当前阶段

阶段 4：NE555 测频软件准备（进行中）

## 当前执行计划

- 项目完整执行顺序见 `Memory/roadmap.md`。
- 已具备母头杜邦线和数字万用表，可重新开始需要外部连线的硬件调试。
- 按键硬件验证已完成；当前优先执行路线图阶段 0 的硬件信息确认，以及阶段 1 的软件接口与算法基础。
- OLED 已完成硬件验证；当前可继续测频软件、内部已知频率自测和 NE555 静态电路设计。

## 已完成并验证

- 已按标准外设库建立项目目录和库文件结构。
- 已建立 Keil 所需的启动文件、CMSIS 文件和 SPL 源文件。
- `System/delay.c/.h` 已通过板载运行验证，SysTick 毫秒延时正常。
- `Hardware/bsp_led.c/.h` 已通过板载运行验证，PC13 每隔约 0.5 秒闪烁。
- 已确认核心板 PWR 红色 LED 常亮，供电正常。
- 已初始化本地 Git 仓库，并建立忽略规则、提交规范和项目 Memory 区域。
- USART1 调试输出已通过硬件验证：启动信息、每秒心跳输出和 PC13 LED 闪烁均正常。

## 当前待做

1. 确认 AT24C08 的封装、A0/A1/A2、WP、供电和 I2C 引脚，再实现 EEPROM 驱动与标定表保存。
2. 确认蜂鸣器的实际引脚和有效电平。
3. 在元件到位后完成最终 NE555 传感器电路和 5V 至 3.3V 电平转换的静态检查。

## 测频驱动验证记录

- 已实现：TIM2_CH1/PA0 上升沿输入捕获、1 MHz 计时基准、溢出处理和 PB0/TIM3_CH3 自测方波输出。
- 已验证：用户已将 `bsp_freq.c` 加入 Keil 的 Hardware 分组，F7 编译与链接结果为 0 Error(s), 0 Warning(s)。
- 已验证：PB0 输出约 1 kHz 方波回接 PA0 后，串口稳定报告 `Frequency: 1000 Hz`。
- 待验证：NE555 输出经 3.3 V 电平转换后接入 PA0 的真实测频联调。

## 已锁定任务：等待波形测量设备

以下任务需要示波器或逻辑分析仪，当前不主动执行、不作为模块完成条件，也不根据 MCU 单独读数宣称通过：

1. NE555 输出方波的幅度、占空比、上升沿、下降沿、毛刺和振铃检查。
2. 5 V 至 3.3 V 电平转换后的高速边沿与过冲检查。
3. 软件 I2C 与 OLED 总线时序的波形质量检查。
4. 铜箔电容传感器在纸张变化时的原始波形稳定性和噪声来源分析。

解锁条件：用户拥有示波器或逻辑分析仪，并明确要求恢复对应任务。

## UI 模块验证记录

- 已实现：监测页、标定页、保存确认页，及按键事件与测量数据状态接口。
- 已验证：用户已将 `app_ui.c` 加入 Keil 的 Application 分组，F7 编译与链接结果为 0 Error(s), 0 Warning(s)。
- 已验证：OLED 显示监测页、标定页和保存确认页均正确；K1-K4 的顺序与乱序规则符合预期。
- 已验证：标定状态在重新进入标定页时正确清除；后台测频更新不会覆盖标定页或确认页文字。
- 结论：按键、UI 状态、OLED 和串口联合流程已通过真实硬件验证。

## 软件 I2C 验证记录

- 已实现：PB6 为 SCL、PB7 为 SDA 的开漏软件 I2C 时序。
- 已验证：用户已将 `bsp_soft_i2c.c` 加入 Keil 的 Hardware 分组，F7 编译与链接结果为 0 Error(s), 0 Warning(s)。
- 已验证：OLED 接线、SSD1306 地址 ACK 和初始化测试通过。

## OLED 驱动验证记录

- 已实现：SSD1306 初始化、0x3C 地址 ACK、清屏、基础字符/字符串/数字显示。
- 已验证：用户已将 `bsp_oled.c` 加入 Keil 的 Hardware 分组，F7 编译与链接结果为 0 Error(s), 0 Warning(s)。
- 已验证：用户下载程序后 OLED 成功点亮；`OLEDK 0123456789` 全部显示正确，随后恢复正式提示 `OLED OK`。
- 结论：SSD1306 驱动、0x3C 地址、PB6/PB7 软件 I2C 和当前字库已通过真实硬件验证。

## 纸张计数模块验证记录

- 已实现：频率偏差阈值、连续稳定样本数、波动重置和标定状态映射。
- 已验证：本机 GCC 算法测试通过，覆盖未初始化、空标定表、稳定等待、波动重置和频率高端越界。
- 已验证：用户已将 `app_paper_counter.c` 加入 Keil 的 Application 分组，F7 编译与链接结果为 0 Error(s), 0 Warning(s)。
- 待验证：NE555 测频驱动完成后，使用真实频率样本调整稳定判定参数并验证张数输出。

## 标定模块验证记录

- 已实现：标定表保存、频率升序校验、范围外状态和整数分段线性插值。
- 已验证：本机 GCC 算法测试通过，覆盖空表、递增/递减张数插值、低端越界和未排序标定表。
- 已验证：用户已将 `app_calibration.c` 加入 Keil 的 Application 分组，F7 编译结果为 0 Error(s)。
- 待验证：NE555 测频驱动完成后，使用真实纸张采集的频率数据验证标定表换算结果。

## 按键硬件验证记录

- K1-K4：已完成硬件接线和实测。
- 已验证：Keil 编译、PB12-PB15 未按下约 3.3 V/按下约 0 V、串口按键事件和 LED 运行均正常。
- 结论：`Hardware/bsp_key.c/.h` 的按键输入和消抖功能已通过硬件验证。
- 后续硬件调试说明必须遵循 `Memory/hardware_debug_standard.md`。

## 本阶段排查记录

- 已验证：仅恢复 `USART1_Init(115200U)`，且不执行发送函数或 `printf` 时，PC13 LED 仍正常闪烁。
- 结论：USART1 初始化路径正常；排查重点转为发送函数和 `printf` 重定向路径。
- 已验证：使用带 TXE 超时的 `USART1_SendString("USART test\r\n")` 后，PC13 LED 正常闪烁，串口正确接收文本。
- 结论：USART1 直接发送路径正常；当前只验证 `printf` 重定向。
- 已验证：使用 `printf("printf test\r\n")` 后，PC13 LED 停止闪烁且串口无输出。
- 结论：问题位于 Keil C 库的 `printf` 输出路径，推定为半主机输出阻塞；直接 USART 发送功能未受影响。
- 已验证：Arm Compiler 6.24 不支持 Arm Compiler 5 的 `#pragma import`，且其标准库已定义 `struct __FILE`。
- 结论：半主机禁用代码必须按 Arm Compiler 6 的接口实现，不能直接使用旧版 Keil 示例。
- 已验证：采用 Arm Compiler 6 的 `__use_no_semihosting` 符号后，`printf("printf test\r\n")` 正常输出，PC13 LED 正常闪烁。
- 结论：半主机阻塞已消除，`printf` 重定向路径正常。
- 已验证：启动信息、每秒一次的串口心跳输出和 PC13 LED 闪烁均正常。
- 结论：USART1 调试模块完成，可用于后续外设的串口调试。

## 后续计划

1. 实现按键模块并验证按键读取。
2. 实现软件 I2C 和 OLED 显示。
3. 实现 NE555 方波测频。
4. 实现频率-张数标定与滤波。
5. 实现 EEPROM 保存标定表。

## 最近验证

- 已验证：加入 `Hardware/bsp_key.c` 后 Keil 编译成功，四个按键输入、LED 和串口运行均正常。
- 结论：按键驱动已通过编译和真实硬件验证。

## 阻塞项

- 母头杜邦线缺失的阻塞项已解除。
- AT24C08、蜂鸣器和最终 NE555 传感器电路的型号、引脚与供电连接仍待确认。
- 波形质量相关任务已按用户要求锁定，等待示波器或逻辑分析仪后再恢复。
